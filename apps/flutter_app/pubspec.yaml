name: vms_flutter_app
description: "Vehicle Management System - Emission Test Booking Platform"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  
  # Supabase
  supabase_flutter: ^2.3.0
  
  # UI/UX
  cupertino_icons: ^1.0.8
  
  # State Management (optional - add later if needed)
  # provider: ^6.1.1
  
  # Local Storage
  # shared_preferences: ^2.2.2
  
  # Utils
  # intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
  # Add custom fonts here if needed
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-Bold.ttf
  #         weight: 700